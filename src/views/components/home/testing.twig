<section class="block">
  <div class="flex flex-wrap gap-4">
    <h2>{{ title }}</h2>
    {% for image in component.collection %}
    <div>
      <p>
        {{ image.description }}
      </p>
      <img src="{{ image.value }}" alt="{{ image.label | default('Image') }}" />
    </div>
    {% endfor %}
  </div>
</section>
